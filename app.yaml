runtime: python39

handlers:
- url: /
  static_files: index.html
  upload: index.html
  expiration: "1h"

- url: /(.*)
  static_files: \1
  upload: (.*)
  expiration: "1h"

automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 0
  max_instances: 1
